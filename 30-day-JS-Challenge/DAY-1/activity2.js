// Declare a variable using const, assign it a boolean value, and log the vlaue to the console.

const bool1 = true;
const bool2 = false;

console.log(`we have two boolean values. They are ${bool1} and ${bool2}`);